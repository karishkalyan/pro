#include<stdio.h>
#include<string.h>
int main()
{
char ch[100],i,b,sum=0,c;
printf("\n enter a string:");
gets(ch);
b=strlen(ch);
for(i=0;i<b;i++)
{
if(a[i]=='.')
sum=sum+1;
}
c=sum+1
printf("\n the no of spaces is %d",sum);
return0;
}
