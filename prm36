
#include<stdio.h>
#include<string.h>
void main()
{
char ch[100],i,b,sum=0;
printf("\n enter a string:");
gets(ch);
b=strlen(ch);
for(i=0;i<b;i++)
{
if(!((0<=a[i]&&(a[i]<=9))||(('a'<=a[i]&&(a[i]<='z')))
sum=sum+1;
}
printf("\n the no of numerics is %d",sum);
getch();
}
